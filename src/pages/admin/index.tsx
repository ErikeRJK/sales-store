import { PageWrapper } from "@/components/PageWrapper";

export default function Admin(){
    return (
        <PageWrapper.Root>
            <PageWrapper.Header/>
            <p>Admin</p>
        </PageWrapper.Root>
    )
}